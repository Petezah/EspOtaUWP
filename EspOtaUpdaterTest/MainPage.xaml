<Page
    x:Class="EspOtaUpdaterTest.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EspOtaUpdaterTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock x:Name="progressText"></TextBlock>
            <ProgressBar x:Name="progressBar"/>
            <TextBox x:Name="uriEntry" PlaceholderText="Enter Uri to POST firmware to"></TextBox>
            <TextBox x:Name="usernameEntry" PlaceholderText="Enter username"></TextBox>
            <PasswordBox x:Name="passwordEntry" PlaceholderText="Enter password" IsPasswordRevealButtonEnabled="True"></PasswordBox>
            <Button Click="Button_Click">Update the firmware</Button>
        </StackPanel>
    </Grid>
</Page>
